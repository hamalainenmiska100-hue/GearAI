# Gear AI Web App

A modern single-page AI web app concept for **Gear**, including the provided SVG logo.

## Run locally

Open `index.html` directly in your browser, or serve the folder:

```bash
python3 -m http.server 8080
```

Then visit <http://localhost:8080>.
